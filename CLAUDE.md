# CLAUDE.md — pgBrain architecture & conventions

This file is read at the start of every Claude Code session in this repo. Keep it as the **current state** of the project, not a changelog. Use `PLAN.md` for "done / next / backlog" and commit history for the diff record.

## Product in one line
Native macOS PostgreSQL GUI that copies JetBrains DataGrip's professional flow (windows-per-connection, tabs, data grids, SQL scratchpad) while feeling Mac-native (SwiftUI chrome, AppKit grids, system menu/dock/window decorations).

## Sibling docs
- **PLAN.md** — iteration-by-iteration changelog (what's done, what's next).
- **RELEASE.md** — release runbook. Read before running `scripts/release.sh`. Covers prereqs, the 10 steps the script performs, flags, troubleshooting, and what Claude already knows when the user asks for a release.

## Foundational decisions (locked 2026-05-20)
| | |
|---|---|
| Display name | **pgBrain** |
| Bundle identifier | `cloud.souris.pgbrain` |
| Min macOS | **15.0 Sequoia** |
| Build host | Xcode 26.5 / Swift 6.3 (Swift 6 language mode, strict concurrency) |
| UI framework | SwiftUI for chrome (Welcome, Settings, About, sheets, menus). AppKit (NSTableView / NSOutlineView) for the data grid and any high-density list. |
| Postgres driver | **PostgresNIO** (added in iter-2). Pure Swift, async/await, no libpq. |
| Build system | **SwiftPM** — `Package.swift` only. `scripts/bundle.sh` wraps the SPM executable into a `.app`. No `.xcodeproj`, no XcodeGen, no Tuist. |
| Sandbox | Not sandboxed. Hardened runtime only. |
| Distribution | DMG + Developer ID + notarization (later). Auto-update via **Sparkle** from GitHub Releases (later). |

## Repo layout
```
pgbrain/
├── Package.swift                  # SwiftPM manifest, macOS 15 target
├── Sources/pgBrain/
│   ├── App/
│   │   ├── pgBrainApp.swift       # @main SwiftUI App (just a Settings scene + commands)
│   │   ├── AppDelegate.swift      # owns WindowManager + menu bar; opens Welcome/About
│   │   ├── AppearanceTokens.swift # Tokens.Spacing/Corner/Window/Brand
│   │   └── AppInfo.swift          # version/build from Bundle.main
│   ├── Windows/
│   │   ├── WelcomeWindow.swift    # NSWindow factory + WindowCloseObserver helper
│   │   └── AboutWindow.swift
│   ├── Views/                     # SwiftUI views (WelcomeView, AboutView, SettingsPlaceholderView)
│   ├── Menu/MenuBarController.swift  # NSStatusItem + dropdown
│   ├── Models/Connection.swift    # connection definition (no password in struct)
│   └── State/WindowManager.swift  # tracks connection NSWindows
├── Resources/                     # .app bundle resources (NOT SwiftPM resources)
│   ├── Info.plist                 # CFBundleIdentifier, LSMinimumSystemVersion=15.0
│   ├── pgBrain.entitlements       # hardened-runtime entitlements, no sandbox
│   └── AppIcon.icns               # generated by scripts/gen-icon.swift
├── scripts/
│   ├── bundle.sh                  # swift build → assemble .app → ad-hoc sign
│   ├── run.sh                     # bundle.sh + open
│   ├── clean.sh                   # nukes .build/ and build/
│   └── gen-icon.swift             # CoreGraphics-drawn AppIcon.icns generator
├── build/                         # build output (gitignored)
├── CLAUDE.md                      # this file
├── PLAN.md                        # what's done / next / backlog
└── README.md                      # how to build & run
```

There are **two** notions of "Resources":
- `Resources/` at repo root → copied into the `.app` bundle by `bundle.sh` (Info.plist, AppIcon.icns, entitlements).
- `Sources/pgBrain/Resources/` → SwiftPM resource bundle, accessed via `Bundle.module`. Currently absent (no SPM resources needed yet).

## Build & run
```bash
./scripts/bundle.sh           # debug build → build/pgBrain.app
./scripts/bundle.sh release   # release build
./scripts/run.sh              # bundle + launch
./scripts/clean.sh
```
First run regenerates `Resources/AppIcon.icns` if the generator is newer.

## Testing
```bash
swift test                                            # local pgbrain_demo as $USER
PGBRAIN_TEST_DSN=postgres://u:p@host:5432/db swift test  # explicit target
```
- **`Tests/pgBrainTests/`** — tier-1 (data-layer) E2E. `@testable import pgBrain`
  drives the real query engines against a live Postgres in throwaway schemas.
- **`TestDB`** picks the target from `PGBRAIN_TEST_DSN` (a `postgres://…` URL) or
  falls back to a local `pgbrain_demo`. **No database reachable → tests SKIP**
  (5s-bounded probe), they don't fail — so `swift test` is green in CI and the
  release preflight gate (`scripts/release.sh`) passes on a DB-less box while
  still running the real clone tests whenever a DB is present.
- Engines meant to be E2E-tested expose a **pure entrypoint** with no
  `ConnectionService`/UI dependency (e.g. `SchemaDuplicator.duplicate(client:…)`);
  the `@MainActor` UI wrapper just adds operations tracking on top. Follow that
  split when adding tests for other engines.
- UI-driving E2E (XCUITest) is **not** here — it needs an Xcode UI-test bundle,
  which collides with the no-`.xcodeproj` rule. Deferred deliberately (see PLAN).

## Window model (DataGrip-style)
- **Welcome window** is shown at launch and whenever there are no other windows.
- **One window per connection**. Closing all connection windows → Welcome reappears (handled by `AppDelegate.applicationShouldHandleReopen` + `WindowManager.connectionWindows` tracking).
- `applicationShouldTerminateAfterLastWindowClosed` returns `false` — the menu bar item keeps the app alive.
- Window factories return `NSWindow` and use the `WindowCloseObserver` helper (`Windows/WelcomeWindow.swift`) to fire a `@MainActor` close callback without forcing strong references.

## SwiftUI ↔ AppKit boundary
- SwiftUI views are hosted in `NSHostingController` and wrapped in plain `NSWindow`s — **not** SwiftUI `WindowGroup`. This is deliberate: we need fine control over title bars, restoration, and per-connection identity that SwiftUI's window scenes don't cleanly express.
- The only SwiftUI `Scene` in `pgBrainApp` is `Settings { … }` — present so `⌘,` works for free and so we get the standard app menu.

## Concurrency
Swift 6 language mode is on. All UI-facing types are `@MainActor`. `WindowCloseObserver`'s callback is typed `@escaping @MainActor () -> Void` — pass closures from `AppDelegate` (already `@MainActor`) directly.

## Tokens & branding
Brand colors live in `AppearanceTokens.Brand`. Primary is a deep violet (`#6B52DB` approx). Used by the gradient on Welcome's left pane and on the About icon. **Icons designed for macOS Sequoia tinting**: use simple monochromatic foregrounds on a flat or single-direction-gradient background. The current `gen-icon.swift` draws a stacked-cylinder mark in white over the brand gradient — safe under tinting.

## Conventions
- No `.xcodeproj`. If you need to open the app in Xcode for debugging, "File → Open Package…" pointed at this directory works.
- No comments that explain *what* the code does. Only *why*-comments when a non-obvious constraint is at play.
- Don't introduce `print()` for runtime logging — when we add logging in iter-3+, use `os.Logger` with the subsystem `cloud.souris.pgbrain`.
- When a feature spans more than one file, **add an entry to `PLAN.md`** alongside the code change.

## What's not built yet
Everything below the Welcome window. iter-2 onward delivers: real connection editor, keychain-stored passwords, PostgresNIO-backed connect, connection windows with sidebar, table-view tabs, SQL scratchpad with inline results, import/export, pg_dump/restore, cross-DB copy with mapping, state restoration, status footer, Sparkle, DMG release pipeline. Track these in `PLAN.md`.
